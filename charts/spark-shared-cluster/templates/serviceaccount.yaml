{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "spark-shared-cluster.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
{{- end }}
